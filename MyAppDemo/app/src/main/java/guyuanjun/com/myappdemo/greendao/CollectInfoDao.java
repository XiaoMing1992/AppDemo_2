package guyuanjun.com.myappdemo.greendao;

import android.database.Cursor;
import android.database.sqlite.SQLiteStatement;

import org.greenrobot.greendao.AbstractDao;
import org.greenrobot.greendao.Property;
import org.greenrobot.greendao.internal.DaoConfig;
import org.greenrobot.greendao.database.Database;
import org.greenrobot.greendao.database.DatabaseStatement;

import guyuanjun.com.myappdemo.bean.CollectInfo;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "COLLECT_INFO".
*/
public class CollectInfoDao extends AbstractDao<CollectInfo, Long> {

    public static final String TABLENAME = "COLLECT_INFO";

    /**
     * Properties of entity CollectInfo.<br/>
     * Can be used for QueryBuilder and for referencing column names.
     */
    public static class Properties {
        public final static Property Id = new Property(0, Long.class, "id", true, "_id");
        public final static Property User_id = new Property(1, Long.class, "user_id", false, "USER_ID");
        public final static Property Item_id = new Property(2, Long.class, "item_id", false, "ITEM_ID");
        public final static Property Collect_time = new Property(3, java.util.Date.class, "collect_time", false, "COLLECT_TIME");
        public final static Property Has_collected = new Property(4, boolean.class, "has_collected", false, "HAS_COLLECTED");
    }


    public CollectInfoDao(DaoConfig config) {
        super(config);
    }
    
    public CollectInfoDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(Database db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"COLLECT_INFO\" (" + //
                "\"_id\" INTEGER PRIMARY KEY AUTOINCREMENT ," + // 0: id
                "\"USER_ID\" INTEGER," + // 1: user_id
                "\"ITEM_ID\" INTEGER," + // 2: item_id
                "\"COLLECT_TIME\" INTEGER," + // 3: collect_time
                "\"HAS_COLLECTED\" INTEGER NOT NULL );"); // 4: has_collected
    }

    /** Drops the underlying database table. */
    public static void dropTable(Database db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"COLLECT_INFO\"";
        db.execSQL(sql);
    }

    @Override
    protected final void bindValues(DatabaseStatement stmt, CollectInfo entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
 
        Long user_id = entity.getUser_id();
        if (user_id != null) {
            stmt.bindLong(2, user_id);
        }
 
        Long item_id = entity.getItem_id();
        if (item_id != null) {
            stmt.bindLong(3, item_id);
        }
 
        java.util.Date collect_time = entity.getCollect_time();
        if (collect_time != null) {
            stmt.bindLong(4, collect_time.getTime());
        }
        stmt.bindLong(5, entity.getHas_collected() ? 1L: 0L);
    }

    @Override
    protected final void bindValues(SQLiteStatement stmt, CollectInfo entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
 
        Long user_id = entity.getUser_id();
        if (user_id != null) {
            stmt.bindLong(2, user_id);
        }
 
        Long item_id = entity.getItem_id();
        if (item_id != null) {
            stmt.bindLong(3, item_id);
        }
 
        java.util.Date collect_time = entity.getCollect_time();
        if (collect_time != null) {
            stmt.bindLong(4, collect_time.getTime());
        }
        stmt.bindLong(5, entity.getHas_collected() ? 1L: 0L);
    }

    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }    

    @Override
    public CollectInfo readEntity(Cursor cursor, int offset) {
        CollectInfo entity = new CollectInfo();
        readEntity(cursor, entity, offset);
        return entity;
    }
     
    @Override
    public void readEntity(Cursor cursor, CollectInfo entity, int offset) {
        entity.setId(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setUser_id(cursor.isNull(offset + 1) ? null : cursor.getLong(offset + 1));
        entity.setItem_id(cursor.isNull(offset + 2) ? null : cursor.getLong(offset + 2));
        entity.setCollect_time(cursor.isNull(offset + 3) ? null : new java.util.Date(cursor.getLong(offset + 3)));
        entity.setHas_collected(cursor.getShort(offset + 4) != 0);
     }
    
    @Override
    protected final Long updateKeyAfterInsert(CollectInfo entity, long rowId) {
        entity.setId(rowId);
        return rowId;
    }
    
    @Override
    public Long getKey(CollectInfo entity) {
        if(entity != null) {
            return entity.getId();
        } else {
            return null;
        }
    }

    @Override
    public boolean hasKey(CollectInfo entity) {
        return entity.getId() != null;
    }

    @Override
    protected final boolean isEntityUpdateable() {
        return true;
    }
    
}
